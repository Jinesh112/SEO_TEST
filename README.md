# Simple React App (No SEO Meta Tags)

This is a minimal React app created with Vite, intentionally **without SEO-related meta tags**.

## Install

```bash
npm install
```

## Run in development

```bash
npm run dev
```

Then open the URL shown in the terminal (by default `http://localhost:5173`).

